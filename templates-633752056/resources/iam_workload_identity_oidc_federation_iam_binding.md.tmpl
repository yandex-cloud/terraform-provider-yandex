---
subcategory: "Identity and Access Management (IAM)"
page_title: "Yandex: {{.Name}}"
description: |-
  Allows management of a single IAM binding for a Yandex IAM workload identity OIDC federation.
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

## Example usage

{{ tffile "examples/iam_workload_identity_oidc_federation_iam_binding/r_iam_workload_identity_oidc_federation_iam_binding_1.tf" }}

{{ .SchemaMarkdown | trimspace }}

## Import

The resource can be imported by using their `resource ID`. For getting the resource ID you can use Yandex Cloud [Web Console](https://console.yandex.cloud) or [YC CLI](https://yandex.cloud/docs/cli/quickstart).

Workload identity OIDC federation binding resource can be imported using the workload identity OIDC federation ID and resource role.

{{ codefile "shell" "examples/iam_workload_identity_oidc_federation_iam_binding/import.sh" }}
