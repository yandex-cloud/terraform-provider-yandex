## 0.221.0 (August 13, 2026)

##### FEATURES:
* resourcemanager: add `yandex_resource_manager_cloud_iam_policy_binding` resource
* triggers: add `yandex_serverless_triggers` resource
* organizationmanager: add `yandex_organization_manager_organization_iam_policy_binding` resource
* resourcemanager: add `yandex_resource_manager_folder_iam_policy_binding` resource
* triggers: add `yandex_serverless_triggers` data source
* postgresql: add `managed_repack` config block to `yandex_mdb_postgresql_cluster` and `yandex_mdb_postgresql_cluster_v2` resources
* mdb_clickhouse: support external shards in shard groups
* serverless: added new resources `yandex_api_gateway_iam_binding` `yandex_api_gateway_iam_member` `yandex_function_iam_member` `yandex_serverless_container_iam_member` `yandex_serverless_workflow_iam_member`
* cloudregistry: add `yandex_cloudregistry_registry_iam_member` and `yandex_cloudregistry_folder_iam_member` resources
* postgresql: add advanced_mode option to yandex_mdb_postgresql_cluster and yandex_mdb_postgresql_cluster_v2 resources
* mdb_clickhouse: support default user settings in `yandex_mdb_clickhouse_cluster_v2` resource and data source

##### BUG FIXES:
* crypto: Fixed missing certificate challenge type after import
* Valkey: Fixed issue when passed falsy notify keyspace events field
* mdb_clickhouse: do not log user passwords, cluster admin password and external dictionary credentials in `yandex_mdb_clickhouse_user` and `yandex_mdb_clickhouse_cluster_v2`

##### ENHANCEMENTS:
* serverless: change `member` in `yandex_api_gateway_iam_member` resource
* smartwebsecurity: add `advanced_rate_limiter_rule.dynamic_quota.ban_period` to `yandex_sws_advanced_rate_limiter_profile` resource
* resourcemanager: change `role` in `yandex_resourcemanager_cloud_iam_member` resource
* lockbox: change `member` in `yandex_lockbox_secret_iam_member` resource
* serverless: change `role` in `yandex_function_iam_member` resource
* kms: change `role` in `yandex_kms_asymmetric_encryption_key_iam_member` resource
* organizationmanager: change `member` in `yandex_organizationmanager_organization_iam_member` resource
* resourcemanager: change `member` in `yandex_resourcemanager_cloud_iam_member` resource
* kms: change `role` in `yandex_kms_asymmetric_signature_key_iam_member` resource
* kms: change `member` in `yandex_kms_asymmetric_encryption_key_iam_member` resource
* serverless: change `role` in `yandex_serverless_container_iam_member` resource
* kms: change `member` in `yandex_kms_asymmetric_signature_key_iam_member` resource
* serverless: change `role` in `yandex_api_gateway_iam_member` resource
* organizationmanager: change `member` in `yandex_organizationmanager_group_iam_member` resource
* serverless: change `role` in `yandex_serverless_workflow_iam_member` resource
* lockbox: change `role` in `yandex_lockbox_secret_iam_member` resource
* serverless: change `member` in `yandex_serverless_container_iam_member` resource
* certificatemanager: change `member` in `yandex_cm_certificate_iam_member` resource
* k8s: change `role` in `yandex_kubernetes_cluster_iam_member` resource
* serverless: change `member` in `yandex_serverless_workflow_iam_member` resource
* iam: change `role` in `yandex_iam_service_account_iam_member` resource
* smartwebsecurity: add `advanced_rate_limiter_rule.dynamic_quota.ban_period` to `yandex_sws_advanced_rate_limiter_profile` data source
* organizationmanager: change `role` in `yandex_organizationmanager_organization_iam_member` resource
* kms: change `role` in `yandex_kms_symmetric_key_iam_member` resource
* resourcemanager: change `role` in `yandex_resourcemanager_folder_iam_member` resource
* organizationmanager: change `role` in `yandex_organizationmanager_group_iam_member` resource
* resourcemanager: change `member` in `yandex_resourcemanager_folder_iam_member` resource
* serverless: change `member` in `yandex_function_iam_member` resource
* certificatemanager: change `role` in `yandex_cm_certificate_iam_member` resource
* iam: change `member` in `yandex_iam_service_account_iam_member` resource
* smartwebsecurity: change `log_options.outputs` in `yandex_sws_security_profile` resource
* kms: change `member` in `yandex_kms_symmetric_key_iam_member` resource
* k8s: change `member` in `yandex_kubernetes_cluster_iam_member` resource
* smartwebsecurity: change `log_options.outputs` in `yandex_sws_security_profile` data source
* opensearch: add write-only admin_password_wo and admin_password_wo_version attributes
* mysql: add write-only password_wo and password_wo_version attributes to yandex_mdb_mysql_user
* valkey: add write-only password attributes to yandex_mdb_redis_cluster, yandex_mdb_redis_cluster_v2 and yandex_mdb_redis_user
* greenplum: add write-only password attributes to yandex_mdb_greenplum_cluster, yandex_mdb_greenplum_cluster_v2, and yandex_mdb_greenplum_user resources
* serverless: improved stability of yandex_function_trigger tests
* smartwebsecurity: add acceptance coverage for server defaults, omitted request body size, ARL detachment, and WAF case sensitivity

##### WARNING:
* valkey: deprecate yandex_mdb_redis_user sanitize-payload setting



