// Code generated by tfgen. DO NOT EDIT.

package yandex_sws_advanced_rate_limiter_profile

import (
	"context"
	"fmt"

	"github.com/hashicorp/terraform-plugin-framework/path"
	"github.com/hashicorp/terraform-plugin-framework/resource"
	"github.com/hashicorp/terraform-plugin-framework/types"
	"github.com/hashicorp/terraform-plugin-log/tflog"
	advanced_rate_limiter "github.com/yandex-cloud/go-genproto/yandex/cloud/smartwebsecurity/v1/advanced_rate_limiter"
	advancedratelimitersdk "github.com/yandex-cloud/go-sdk/services/smartwebsecurity/v1/advanced_rate_limiter"
	"github.com/yandex-cloud/terraform-provider-yandex/pkg/converter"
	"github.com/yandex-cloud/terraform-provider-yandex/pkg/validate"
	provider_config "github.com/yandex-cloud/terraform-provider-yandex/yandex-framework/provider/config"
	"google.golang.org/genproto/protobuf/field_mask"
	"google.golang.org/grpc"
	"google.golang.org/grpc/codes"
	"google.golang.org/grpc/metadata"
)

var _ resource.ResourceWithConfigure = (*yandexSwsAdvancedRateLimiterProfileResource)(nil)
var _ resource.ResourceWithImportState = (*yandexSwsAdvancedRateLimiterProfileResource)(nil)

type yandexSwsAdvancedRateLimiterProfileResource struct {
	providerConfig *provider_config.Config
}

func NewResource() resource.Resource {
	return &yandexSwsAdvancedRateLimiterProfileResource{}
}

func (r *yandexSwsAdvancedRateLimiterProfileResource) Metadata(_ context.Context, req resource.MetadataRequest, resp *resource.MetadataResponse) {
	resp.TypeName = "yandex_sws_advanced_rate_limiter_profile"
}

func (r *yandexSwsAdvancedRateLimiterProfileResource) Configure(_ context.Context, req resource.ConfigureRequest, resp *resource.ConfigureResponse) {
	if req.ProviderData == nil {
		return
	}

	providerConfig, ok := req.ProviderData.(*provider_config.Config)
	if !ok {
		resp.Diagnostics.AddError(
			"Unexpected Resource Configure Type",
			fmt.Sprintf("Expected *provider_config.Config, got: %T. Please report this issue to the provider developers.", req.ProviderData),
		)
		return
	}

	r.providerConfig = providerConfig
}

func (r *yandexSwsAdvancedRateLimiterProfileResource) Schema(ctx context.Context, _ resource.SchemaRequest, resp *resource.SchemaResponse) {
	resp.Schema = YandexSwsAdvancedRateLimiterProfileResourceSchema(ctx)
}

func (r *yandexSwsAdvancedRateLimiterProfileResource) ImportState(ctx context.Context, req resource.ImportStateRequest, resp *resource.ImportStateResponse) {
	resource.ImportStatePassthroughID(ctx, path.Root("advanced_rate_limiter_profile_id"), req, resp)
}

func (r *yandexSwsAdvancedRateLimiterProfileResource) Read(ctx context.Context, req resource.ReadRequest, resp *resource.ReadResponse) {
	var state yandexSwsAdvancedRateLimiterProfileModel
	diags := req.State.Get(ctx, &state)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	readTimeout, timeoutInitError := state.Timeouts.Read(ctx, provider_config.DefaultTimeout)
	if timeoutInitError != nil {
		resp.Diagnostics.Append(timeoutInitError...)
		return
	}

	ctx, cancel := context.WithTimeout(ctx, readTimeout)
	defer cancel()

	reqApi := &advanced_rate_limiter.GetAdvancedRateLimiterProfileRequest{}
	id := state.ID.ValueString()
	if !state.AdvancedRateLimiterProfileId.IsUnknown() && !state.AdvancedRateLimiterProfileId.IsNull() {
		id = state.AdvancedRateLimiterProfileId.ValueString()
	}
	reqApi.SetAdvancedRateLimiterProfileId(id)
	tflog.Debug(ctx, fmt.Sprintf("Read advanced_rate_limiter_profile request: %s", validate.ProtoDump(reqApi)))

	md := new(metadata.MD)
	res, err := advancedratelimitersdk.NewAdvancedRateLimiterProfileClient(r.providerConfig.SDKv2).Get(ctx, reqApi, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read advanced_rate_limiter_profile x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read advanced_rate_limiter_profile x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		if validate.IsStatusWithCode(err, codes.NotFound) {
			resp.Diagnostics.AddWarning(
				"Failed to Read resource",
				"advanced_rate_limiter_profile not found",
			)
			resp.State.RemoveResource(ctx)
			return
		} else {
			resp.Diagnostics.AddError(
				"Failed to Read resource",
				"Error while requesting API to get advanced_rate_limiter_profile:"+err.Error(),
			)
		}
	}
	tflog.Debug(ctx, fmt.Sprintf("Read advanced_rate_limiter_profile response: %s", validate.ProtoDump(res)))

	if resp.Diagnostics.HasError() {
		return
	}

	// diagnostics don't have errors and resource is nil => resource not found
	if res == nil {
		resp.Diagnostics.AddError("Failed to read", "Resource not found")
		return
	}

	newState := flattenYandexSwsAdvancedRateLimiterProfile(ctx, res, state, state.Timeouts, &resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &newState)...)
	if resp.Diagnostics.HasError() {
		return
	}
}

func (r *yandexSwsAdvancedRateLimiterProfileResource) Create(ctx context.Context, req resource.CreateRequest, resp *resource.CreateResponse) {
	var plan yandexSwsAdvancedRateLimiterProfileModel
	diags := req.Plan.Get(ctx, &plan)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	createTimeout, timeoutInitError := plan.Timeouts.Create(ctx, provider_config.DefaultTimeout)
	if timeoutInitError != nil {
		resp.Diagnostics.Append(timeoutInitError...)
		return
	}

	ctx, cancel := context.WithTimeout(ctx, createTimeout)
	defer cancel()

	createReq := &advanced_rate_limiter.CreateAdvancedRateLimiterProfileRequest{}
	createReq.SetFolderId(converter.GetFolderID(plan.FolderId.ValueString(), r.providerConfig, &diags))
	createReq.SetLabels(expandYandexSwsAdvancedRateLimiterProfileLabels(ctx, plan.Labels, &diags))
	createReq.SetName(plan.Name.ValueString())
	createReq.SetDescription(plan.Description.ValueString())
	createReq.SetAdvancedRateLimiterRules(expandYandexSwsAdvancedRateLimiterProfileAdvancedRateLimiterRule(ctx, plan.AdvancedRateLimiterRule, &diags))
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}
	tflog.Debug(ctx, fmt.Sprintf("Create advanced_rate_limiter_profile request: %s", validate.ProtoDump(createReq)))

	md := new(metadata.MD)
	op, err := advancedratelimitersdk.NewAdvancedRateLimiterProfileClient(r.providerConfig.SDKv2).Create(ctx, createReq, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("[DEBUG] Create advanced_rate_limiter_profile x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Create advanced_rate_limiter_profile x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		resp.Diagnostics.AddError(
			"Failed to Create resource",
			"Error while requesting API to create advanced_rate_limiter_profile:"+err.Error(),
		)
		return
	}
	createRes, err := op.Wait(ctx)
	if err != nil {
		resp.Diagnostics.AddError(
			"Unable to Create Resource",
			fmt.Sprintf("An unexpected error occurred while waiting longrunning response. "+
				"Please retry the operation or report this issue to the provider developers.\n\n"+
				"Error: %s", err),
		)
		return
	}
	tflog.Debug(ctx, fmt.Sprintf("Create advanced_rate_limiter_profile response: %s", validate.ProtoDump(createRes)))

	plan.AdvancedRateLimiterProfileId = types.StringValue(createRes.Id)
	reqApi := &advanced_rate_limiter.GetAdvancedRateLimiterProfileRequest{}
	reqApi.SetAdvancedRateLimiterProfileId(plan.AdvancedRateLimiterProfileId.ValueString())
	tflog.Debug(ctx, fmt.Sprintf("Read advanced_rate_limiter_profile request: %s", validate.ProtoDump(reqApi)))

	md = new(metadata.MD)
	res, err := advancedratelimitersdk.NewAdvancedRateLimiterProfileClient(r.providerConfig.SDKv2).Get(ctx, reqApi, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read advanced_rate_limiter_profile x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read advanced_rate_limiter_profile x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		if validate.IsStatusWithCode(err, codes.NotFound) {
			resp.Diagnostics.AddWarning(
				"Failed to Read resource",
				"advanced_rate_limiter_profile not found",
			)
		} else {
			resp.Diagnostics.AddError(
				"Failed to Read resource",
				"Error while requesting API to get advanced_rate_limiter_profile:"+err.Error(),
			)
		}
	}
	tflog.Debug(ctx, fmt.Sprintf("Read advanced_rate_limiter_profile response: %s", validate.ProtoDump(res)))

	if resp.Diagnostics.HasError() {
		return
	}

	// diagnostics don't have errors and resource is nil => resource not found
	if res == nil {
		resp.Diagnostics.AddError("Failed to read", "Resource not found")
		return
	}

	newState := flattenYandexSwsAdvancedRateLimiterProfile(ctx, res, plan, plan.Timeouts, &resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &newState)...)
	if resp.Diagnostics.HasError() {
		return
	}
}

func (r *yandexSwsAdvancedRateLimiterProfileResource) Delete(ctx context.Context, req resource.DeleteRequest, resp *resource.DeleteResponse) {
	var state yandexSwsAdvancedRateLimiterProfileModel
	diags := req.State.Get(ctx, &state)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	deleteTimeout, timeoutInitError := state.Timeouts.Delete(ctx, provider_config.DefaultTimeout)
	if timeoutInitError != nil {
		resp.Diagnostics.Append(timeoutInitError...)
		return
	}

	ctx, cancel := context.WithTimeout(ctx, deleteTimeout)
	defer cancel()

	reqApi := &advanced_rate_limiter.DeleteAdvancedRateLimiterProfileRequest{}
	id := state.ID.ValueString()
	if !state.AdvancedRateLimiterProfileId.IsUnknown() && !state.AdvancedRateLimiterProfileId.IsNull() {
		id = state.AdvancedRateLimiterProfileId.ValueString()
	}
	reqApi.SetAdvancedRateLimiterProfileId(id)
	tflog.Debug(ctx, fmt.Sprintf("Delete advanced_rate_limiter_profile request: %s", validate.ProtoDump(reqApi)))

	md := new(metadata.MD)

	op, err := advancedratelimitersdk.NewAdvancedRateLimiterProfileClient(r.providerConfig.SDKv2).Delete(ctx, reqApi, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Delete advanced_rate_limiter_profile x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Delete advanced_rate_limiter_profile x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		resp.Diagnostics.AddError(
			"Failed to Delete resource",
			"Error while requesting API to delete advanced_rate_limiter_profile:"+err.Error(),
		)
		return
	}
	deleteRes, err := op.Wait(ctx)
	if err != nil {
		resp.Diagnostics.AddError(
			"Unable to Delete Resource",
			fmt.Sprintf("An unexpected error occurred while waiting longrunning response. "+
				"Please retry the operation or report this issue to the provider developers.\n\n"+
				"Error: %s", err),
		)
		return
	}
	tflog.Debug(ctx, fmt.Sprintf("[DEBUG] Delete advanced_rate_limiter_profile response: %s", validate.ProtoDump(deleteRes)))
}

func (r *yandexSwsAdvancedRateLimiterProfileResource) Update(ctx context.Context, req resource.UpdateRequest, resp *resource.UpdateResponse) {
	var plan, state yandexSwsAdvancedRateLimiterProfileModel
	diags := req.Plan.Get(ctx, &plan)
	resp.Diagnostics.Append(diags...)
	diags = req.State.Get(ctx, &state)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	updateTimeout, timeoutInitError := plan.Timeouts.Update(ctx, provider_config.DefaultTimeout)
	if timeoutInitError != nil {
		resp.Diagnostics.Append(timeoutInitError...)
		return
	}

	ctx, cancel := context.WithTimeout(ctx, updateTimeout)
	defer cancel()
	var updatePaths []string

	if !plan.AdvancedRateLimiterProfileId.Equal(state.AdvancedRateLimiterProfileId) {
		updatePaths = append(updatePaths, "advanced_rate_limiter_profile_id")
	}
	if plan.AdvancedRateLimiterRule.IsNull() {
		plan.AdvancedRateLimiterRule = types.ListNull(yandexSwsAdvancedRateLimiterProfileAdvancedRateLimiterRuleStructModelType)
	}
	if state.AdvancedRateLimiterRule.IsNull() {
		state.AdvancedRateLimiterRule = types.ListNull(yandexSwsAdvancedRateLimiterProfileAdvancedRateLimiterRuleStructModelType)
	}
	if !plan.AdvancedRateLimiterRule.Equal(state.AdvancedRateLimiterRule) {
		updatePaths = append(updatePaths, "advanced_rate_limiter_rules")
	}
	if !plan.Description.Equal(state.Description) {
		updatePaths = append(updatePaths, "description")
	}
	if plan.Labels.IsNull() {
		plan.Labels = types.MapNull(types.StringType)
	}
	if state.Labels.IsNull() {
		state.Labels = types.MapNull(types.StringType)
	}
	if !plan.Labels.Equal(state.Labels) {
		updatePaths = append(updatePaths, "labels")
	}
	if !plan.Name.Equal(state.Name) {
		updatePaths = append(updatePaths, "name")
	}
	if len(updatePaths) != 0 {

		updateReq := &advanced_rate_limiter.UpdateAdvancedRateLimiterProfileRequest{}
		id := plan.ID.ValueString()
		if !plan.AdvancedRateLimiterProfileId.IsUnknown() && !plan.AdvancedRateLimiterProfileId.IsNull() {
			id = plan.AdvancedRateLimiterProfileId.ValueString()
		}
		updateReq.SetAdvancedRateLimiterProfileId(id)
		updateReq.SetLabels(expandYandexSwsAdvancedRateLimiterProfileLabels(ctx, plan.Labels, &diags))
		updateReq.SetName(plan.Name.ValueString())
		updateReq.SetDescription(plan.Description.ValueString())
		updateReq.SetAdvancedRateLimiterRules(expandYandexSwsAdvancedRateLimiterProfileAdvancedRateLimiterRule(ctx, plan.AdvancedRateLimiterRule, &diags))
		updateReq.SetUpdateMask(&field_mask.FieldMask{Paths: updatePaths})

		resp.Diagnostics.Append(diags...)
		if resp.Diagnostics.HasError() {
			return
		}
		tflog.Debug(ctx, fmt.Sprintf("Update advanced_rate_limiter_profile request: %s", validate.ProtoDump(updateReq)))

		md := new(metadata.MD)
		op, err := advancedratelimitersdk.NewAdvancedRateLimiterProfileClient(r.providerConfig.SDKv2).Update(ctx, updateReq, grpc.Header(md))
		if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
			tflog.Debug(ctx, fmt.Sprintf("Update advanced_rate_limiter_profile x-server-trace-id: %s", traceHeader[0]))
		}
		if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
			tflog.Debug(ctx, fmt.Sprintf("Update advanced_rate_limiter_profile x-server-request-id: %s", traceHeader[0]))
		}
		if err != nil {
			resp.Diagnostics.AddError(
				"Failed to Read resource",
				"Error while requesting API to update advanced_rate_limiter_profile:"+err.Error(),
			)
			return
		}
		updateRes, err := op.Wait(ctx)
		if err != nil {
			resp.Diagnostics.AddError(
				"Unable to Update Resource",
				fmt.Sprintf("An unexpected error occurred while waiting longrunning response. "+
					"Please retry the operation or report this issue to the provider developers.\n\n"+
					"Error: %s", err),
			)
			return
		}
		tflog.Debug(ctx, fmt.Sprintf("Update advanced_rate_limiter_profile response: %s", validate.ProtoDump(updateRes)))

		plan.AdvancedRateLimiterProfileId = types.StringValue(updateRes.Id)
	}
	reqApi := &advanced_rate_limiter.GetAdvancedRateLimiterProfileRequest{}
	reqApi.SetAdvancedRateLimiterProfileId(plan.AdvancedRateLimiterProfileId.ValueString())
	tflog.Debug(ctx, fmt.Sprintf("Read advanced_rate_limiter_profile request: %s", validate.ProtoDump(reqApi)))
	md := new(metadata.MD)
	res, err := advancedratelimitersdk.NewAdvancedRateLimiterProfileClient(r.providerConfig.SDKv2).Get(ctx, reqApi, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read advanced_rate_limiter_profile x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read advanced_rate_limiter_profile x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		if validate.IsStatusWithCode(err, codes.NotFound) {
			resp.Diagnostics.AddWarning(
				"Failed to Read resource",
				"advanced_rate_limiter_profile not found",
			)
		} else {
			resp.Diagnostics.AddError(
				"Failed to Read resource",
				"Error while requesting API to get advanced_rate_limiter_profile:"+err.Error(),
			)
		}
	}
	tflog.Debug(ctx, fmt.Sprintf("Read advanced_rate_limiter_profile response: %s", validate.ProtoDump(res)))

	if resp.Diagnostics.HasError() {
		return
	}

	// diagnostics don't have errors and resource is nil => resource not found
	if res == nil {
		resp.Diagnostics.AddError("Failed to read", "Resource not found")
		return
	}

	to := state.Timeouts
	if !plan.Timeouts.Equal(state.Timeouts) {
		to = plan.Timeouts
	}

	newState := flattenYandexSwsAdvancedRateLimiterProfile(ctx, res, plan, to, &resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &newState)...)
	if resp.Diagnostics.HasError() {
		return
	}
}
