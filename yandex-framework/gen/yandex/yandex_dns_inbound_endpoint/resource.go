// Code generated by tfgen. DO NOT EDIT.

package yandex_dns_inbound_endpoint

import (
	"context"
	"fmt"

	"github.com/hashicorp/terraform-plugin-framework/path"
	"github.com/hashicorp/terraform-plugin-framework/resource"
	"github.com/hashicorp/terraform-plugin-framework/types"
	"github.com/hashicorp/terraform-plugin-log/tflog"
	dns "github.com/yandex-cloud/go-genproto/yandex/cloud/dns/v1"
	dnsv1sdk "github.com/yandex-cloud/go-sdk/services/dns/v1"
	"github.com/yandex-cloud/terraform-provider-yandex/pkg/converter"
	"github.com/yandex-cloud/terraform-provider-yandex/pkg/validate"
	provider_config "github.com/yandex-cloud/terraform-provider-yandex/yandex-framework/provider/config"
	"google.golang.org/genproto/protobuf/field_mask"
	"google.golang.org/grpc"
	"google.golang.org/grpc/codes"
	"google.golang.org/grpc/metadata"
)

var _ resource.ResourceWithConfigure = (*yandexDnsInboundEndpointResource)(nil)
var _ resource.ResourceWithImportState = (*yandexDnsInboundEndpointResource)(nil)

type yandexDnsInboundEndpointResource struct {
	providerConfig *provider_config.Config
}

func NewResource() resource.Resource {
	return &yandexDnsInboundEndpointResource{}
}

func (r *yandexDnsInboundEndpointResource) Metadata(_ context.Context, req resource.MetadataRequest, resp *resource.MetadataResponse) {
	resp.TypeName = "yandex_dns_inbound_endpoint"
}

func (r *yandexDnsInboundEndpointResource) Configure(_ context.Context, req resource.ConfigureRequest, resp *resource.ConfigureResponse) {
	if req.ProviderData == nil {
		return
	}

	providerConfig, ok := req.ProviderData.(*provider_config.Config)
	if !ok {
		resp.Diagnostics.AddError(
			"Unexpected Resource Configure Type",
			fmt.Sprintf("Expected *provider_config.Config, got: %T. Please report this issue to the provider developers.", req.ProviderData),
		)
		return
	}

	r.providerConfig = providerConfig
}

func (r *yandexDnsInboundEndpointResource) Schema(ctx context.Context, _ resource.SchemaRequest, resp *resource.SchemaResponse) {
	resp.Schema = YandexDnsInboundEndpointResourceSchema(ctx)
}

func (r *yandexDnsInboundEndpointResource) ImportState(ctx context.Context, req resource.ImportStateRequest, resp *resource.ImportStateResponse) {
	resource.ImportStatePassthroughID(ctx, path.Root("dns_inbound_endpoint_id"), req, resp)
}

func (r *yandexDnsInboundEndpointResource) Read(ctx context.Context, req resource.ReadRequest, resp *resource.ReadResponse) {
	var state yandexDnsInboundEndpointModel
	diags := req.State.Get(ctx, &state)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	readTimeout, timeoutInitError := state.Timeouts.Read(ctx, provider_config.DefaultTimeout)
	if timeoutInitError != nil {
		resp.Diagnostics.Append(timeoutInitError...)
		return
	}

	ctx, cancel := context.WithTimeout(ctx, readTimeout)
	defer cancel()

	reqApi := &dns.GetDnsInboundEndpointRequest{}
	id := state.ID.ValueString()
	if !state.DnsInboundEndpointId.IsUnknown() && !state.DnsInboundEndpointId.IsNull() {
		id = state.DnsInboundEndpointId.ValueString()
	}
	reqApi.SetDnsInboundEndpointId(id)
	tflog.Debug(ctx, fmt.Sprintf("Read dns_inbound_endpoint request: %s", validate.ProtoDump(reqApi)))

	md := new(metadata.MD)
	res, err := dnsv1sdk.NewDnsInboundEndpointClient(r.providerConfig.SDKv2).Get(ctx, reqApi, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read dns_inbound_endpoint x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read dns_inbound_endpoint x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		if validate.IsStatusWithCode(err, codes.NotFound) {
			resp.Diagnostics.AddWarning(
				"Failed to Read resource",
				"dns_inbound_endpoint not found",
			)
			resp.State.RemoveResource(ctx)
			return
		} else {
			resp.Diagnostics.AddError(
				"Failed to Read resource",
				"Error while requesting API to get dns_inbound_endpoint:"+err.Error(),
			)
		}
	}
	tflog.Debug(ctx, fmt.Sprintf("Read dns_inbound_endpoint response: %s", validate.ProtoDump(res)))

	if resp.Diagnostics.HasError() {
		return
	}

	// diagnostics don't have errors and resource is nil => resource not found
	if res == nil {
		resp.Diagnostics.AddError("Failed to read", "Resource not found")
		return
	}

	newState := flattenYandexDnsInboundEndpoint(ctx, res, state, state.Timeouts, &resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &newState)...)
	if resp.Diagnostics.HasError() {
		return
	}
}

func (r *yandexDnsInboundEndpointResource) Create(ctx context.Context, req resource.CreateRequest, resp *resource.CreateResponse) {
	var plan yandexDnsInboundEndpointModel
	diags := req.Plan.Get(ctx, &plan)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	createTimeout, timeoutInitError := plan.Timeouts.Create(ctx, provider_config.DefaultTimeout)
	if timeoutInitError != nil {
		resp.Diagnostics.Append(timeoutInitError...)
		return
	}

	ctx, cancel := context.WithTimeout(ctx, createTimeout)
	defer cancel()

	createReq := &dns.CreateDnsInboundEndpointRequest{}
	createReq.SetFolderId(converter.GetFolderID(plan.FolderId.ValueString(), r.providerConfig, &diags))
	createReq.SetName(plan.Name.ValueString())
	createReq.SetDescription(plan.Description.ValueString())
	createReq.SetLabels(expandYandexDnsInboundEndpointLabels(ctx, plan.Labels, &diags))
	createReq.SetNetworkId(plan.NetworkId.ValueString())
	createReq.SetDeletionProtection(plan.DeletionProtection.ValueBool())
	createReq.SetAddressId(plan.AddressId.ValueString())
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}
	tflog.Debug(ctx, fmt.Sprintf("Create dns_inbound_endpoint request: %s", validate.ProtoDump(createReq)))

	md := new(metadata.MD)
	op, err := dnsv1sdk.NewDnsInboundEndpointClient(r.providerConfig.SDKv2).Create(ctx, createReq, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("[DEBUG] Create dns_inbound_endpoint x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Create dns_inbound_endpoint x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		resp.Diagnostics.AddError(
			"Failed to Create resource",
			"Error while requesting API to create dns_inbound_endpoint:"+err.Error(),
		)
		return
	}
	createRes, err := op.Wait(ctx)
	if err != nil {
		resp.Diagnostics.AddError(
			"Unable to Create Resource",
			fmt.Sprintf("An unexpected error occurred while waiting longrunning response. "+
				"Please retry the operation or report this issue to the provider developers.\n\n"+
				"Error: %s", err),
		)
		return
	}
	tflog.Debug(ctx, fmt.Sprintf("Create dns_inbound_endpoint response: %s", validate.ProtoDump(createRes)))

	plan.DnsInboundEndpointId = types.StringValue(createRes.Id)
	reqApi := &dns.GetDnsInboundEndpointRequest{}
	reqApi.SetDnsInboundEndpointId(plan.DnsInboundEndpointId.ValueString())
	tflog.Debug(ctx, fmt.Sprintf("Read dns_inbound_endpoint request: %s", validate.ProtoDump(reqApi)))

	md = new(metadata.MD)
	res, err := dnsv1sdk.NewDnsInboundEndpointClient(r.providerConfig.SDKv2).Get(ctx, reqApi, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read dns_inbound_endpoint x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read dns_inbound_endpoint x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		if validate.IsStatusWithCode(err, codes.NotFound) {
			resp.Diagnostics.AddWarning(
				"Failed to Read resource",
				"dns_inbound_endpoint not found",
			)
		} else {
			resp.Diagnostics.AddError(
				"Failed to Read resource",
				"Error while requesting API to get dns_inbound_endpoint:"+err.Error(),
			)
		}
	}
	tflog.Debug(ctx, fmt.Sprintf("Read dns_inbound_endpoint response: %s", validate.ProtoDump(res)))

	if resp.Diagnostics.HasError() {
		return
	}

	// diagnostics don't have errors and resource is nil => resource not found
	if res == nil {
		resp.Diagnostics.AddError("Failed to read", "Resource not found")
		return
	}

	newState := flattenYandexDnsInboundEndpoint(ctx, res, plan, plan.Timeouts, &resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &newState)...)
	if resp.Diagnostics.HasError() {
		return
	}
}

func (r *yandexDnsInboundEndpointResource) Delete(ctx context.Context, req resource.DeleteRequest, resp *resource.DeleteResponse) {
	var state yandexDnsInboundEndpointModel
	diags := req.State.Get(ctx, &state)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	deleteTimeout, timeoutInitError := state.Timeouts.Delete(ctx, provider_config.DefaultTimeout)
	if timeoutInitError != nil {
		resp.Diagnostics.Append(timeoutInitError...)
		return
	}

	ctx, cancel := context.WithTimeout(ctx, deleteTimeout)
	defer cancel()

	reqApi := &dns.DeleteDnsInboundEndpointRequest{}
	id := state.ID.ValueString()
	if !state.DnsInboundEndpointId.IsUnknown() && !state.DnsInboundEndpointId.IsNull() {
		id = state.DnsInboundEndpointId.ValueString()
	}
	reqApi.SetDnsInboundEndpointId(id)
	tflog.Debug(ctx, fmt.Sprintf("Delete dns_inbound_endpoint request: %s", validate.ProtoDump(reqApi)))

	md := new(metadata.MD)

	op, err := dnsv1sdk.NewDnsInboundEndpointClient(r.providerConfig.SDKv2).Delete(ctx, reqApi, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Delete dns_inbound_endpoint x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Delete dns_inbound_endpoint x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		resp.Diagnostics.AddError(
			"Failed to Delete resource",
			"Error while requesting API to delete dns_inbound_endpoint:"+err.Error(),
		)
		return
	}
	deleteRes, err := op.Wait(ctx)
	if err != nil {
		resp.Diagnostics.AddError(
			"Unable to Delete Resource",
			fmt.Sprintf("An unexpected error occurred while waiting longrunning response. "+
				"Please retry the operation or report this issue to the provider developers.\n\n"+
				"Error: %s", err),
		)
		return
	}
	tflog.Debug(ctx, fmt.Sprintf("[DEBUG] Delete dns_inbound_endpoint response: %s", validate.ProtoDump(deleteRes)))
}

func (r *yandexDnsInboundEndpointResource) Update(ctx context.Context, req resource.UpdateRequest, resp *resource.UpdateResponse) {
	var plan, state yandexDnsInboundEndpointModel
	diags := req.Plan.Get(ctx, &plan)
	resp.Diagnostics.Append(diags...)
	diags = req.State.Get(ctx, &state)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	updateTimeout, timeoutInitError := plan.Timeouts.Update(ctx, provider_config.DefaultTimeout)
	if timeoutInitError != nil {
		resp.Diagnostics.Append(timeoutInitError...)
		return
	}

	ctx, cancel := context.WithTimeout(ctx, updateTimeout)
	defer cancel()
	var updatePaths []string

	if !plan.DeletionProtection.IsUnknown() && !plan.DeletionProtection.Equal(state.DeletionProtection) {
		updatePaths = append(updatePaths, "deletion_protection")
	}
	if !plan.Description.IsUnknown() && !plan.Description.Equal(state.Description) {
		updatePaths = append(updatePaths, "description")
	}
	if !plan.DnsInboundEndpointId.IsUnknown() && !plan.DnsInboundEndpointId.Equal(state.DnsInboundEndpointId) {
		updatePaths = append(updatePaths, "dns_inbound_endpoint_id")
	}
	if !plan.FolderId.IsUnknown() && !plan.FolderId.Equal(state.FolderId) {
		updatePaths = append(updatePaths, "folder_id")
	}
	if plan.Labels.IsNull() {
		plan.Labels = types.MapNull(types.StringType)
	}
	if state.Labels.IsNull() {
		state.Labels = types.MapNull(types.StringType)
	}
	if !plan.Labels.IsUnknown() && !plan.Labels.Equal(state.Labels) {
		updatePaths = append(updatePaths, "labels")
	}
	if !plan.Name.IsUnknown() && !plan.Name.Equal(state.Name) {
		updatePaths = append(updatePaths, "name")
	}
	if len(updatePaths) != 0 {

		updateReq := &dns.UpdateDnsInboundEndpointRequest{}
		id := plan.ID.ValueString()
		if !plan.DnsInboundEndpointId.IsUnknown() && !plan.DnsInboundEndpointId.IsNull() {
			id = plan.DnsInboundEndpointId.ValueString()
		}
		updateReq.SetDnsInboundEndpointId(id)
		updateReq.SetFolderId(converter.GetFolderID(plan.FolderId.ValueString(), r.providerConfig, &diags))
		updateReq.SetName(plan.Name.ValueString())
		updateReq.SetDescription(plan.Description.ValueString())
		updateReq.SetLabels(expandYandexDnsInboundEndpointLabels(ctx, plan.Labels, &diags))
		updateReq.SetDeletionProtection(plan.DeletionProtection.ValueBool())
		updateReq.SetUpdateMask(&field_mask.FieldMask{Paths: updatePaths})

		resp.Diagnostics.Append(diags...)
		if resp.Diagnostics.HasError() {
			return
		}
		tflog.Debug(ctx, fmt.Sprintf("Update dns_inbound_endpoint request: %s", validate.ProtoDump(updateReq)))

		md := new(metadata.MD)
		op, err := dnsv1sdk.NewDnsInboundEndpointClient(r.providerConfig.SDKv2).Update(ctx, updateReq, grpc.Header(md))
		if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
			tflog.Debug(ctx, fmt.Sprintf("Update dns_inbound_endpoint x-server-trace-id: %s", traceHeader[0]))
		}
		if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
			tflog.Debug(ctx, fmt.Sprintf("Update dns_inbound_endpoint x-server-request-id: %s", traceHeader[0]))
		}
		if err != nil {
			resp.Diagnostics.AddError(
				"Failed to Read resource",
				"Error while requesting API to update dns_inbound_endpoint:"+err.Error(),
			)
			return
		}
		updateRes, err := op.Wait(ctx)
		if err != nil {
			resp.Diagnostics.AddError(
				"Unable to Update Resource",
				fmt.Sprintf("An unexpected error occurred while waiting longrunning response. "+
					"Please retry the operation or report this issue to the provider developers.\n\n"+
					"Error: %s", err),
			)
			return
		}
		tflog.Debug(ctx, fmt.Sprintf("Update dns_inbound_endpoint response: %s", validate.ProtoDump(updateRes)))

		plan.DnsInboundEndpointId = types.StringValue(updateRes.Id)
	}
	reqApi := &dns.GetDnsInboundEndpointRequest{}
	reqApi.SetDnsInboundEndpointId(plan.DnsInboundEndpointId.ValueString())
	tflog.Debug(ctx, fmt.Sprintf("Read dns_inbound_endpoint request: %s", validate.ProtoDump(reqApi)))
	md := new(metadata.MD)
	res, err := dnsv1sdk.NewDnsInboundEndpointClient(r.providerConfig.SDKv2).Get(ctx, reqApi, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read dns_inbound_endpoint x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read dns_inbound_endpoint x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		if validate.IsStatusWithCode(err, codes.NotFound) {
			resp.Diagnostics.AddWarning(
				"Failed to Read resource",
				"dns_inbound_endpoint not found",
			)
		} else {
			resp.Diagnostics.AddError(
				"Failed to Read resource",
				"Error while requesting API to get dns_inbound_endpoint:"+err.Error(),
			)
		}
	}
	tflog.Debug(ctx, fmt.Sprintf("Read dns_inbound_endpoint response: %s", validate.ProtoDump(res)))

	if resp.Diagnostics.HasError() {
		return
	}

	// diagnostics don't have errors and resource is nil => resource not found
	if res == nil {
		resp.Diagnostics.AddError("Failed to read", "Resource not found")
		return
	}

	to := state.Timeouts
	if !plan.Timeouts.Equal(state.Timeouts) {
		to = plan.Timeouts
	}

	newState := flattenYandexDnsInboundEndpoint(ctx, res, plan, to, &resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &newState)...)
	if resp.Diagnostics.HasError() {
		return
	}
}
