// Code generated by tfgen. DO NOT EDIT.

package yandex_kms_asymmetric_signature_key

import (
	"context"
	"fmt"

	"github.com/hashicorp/terraform-plugin-framework/path"
	"github.com/hashicorp/terraform-plugin-framework/resource"
	"github.com/hashicorp/terraform-plugin-framework/types"
	"github.com/hashicorp/terraform-plugin-log/tflog"
	asymmetricsignature "github.com/yandex-cloud/go-genproto/yandex/cloud/kms/v1/asymmetricsignature"
	asymmetricsignaturesdk "github.com/yandex-cloud/go-sdk/services/kms/v1/asymmetricsignature"
	"github.com/yandex-cloud/terraform-provider-yandex/pkg/converter"
	"github.com/yandex-cloud/terraform-provider-yandex/pkg/validate"
	provider_config "github.com/yandex-cloud/terraform-provider-yandex/yandex-framework/provider/config"
	"google.golang.org/genproto/protobuf/field_mask"
	"google.golang.org/grpc"
	"google.golang.org/grpc/codes"
	"google.golang.org/grpc/metadata"
)

var _ resource.ResourceWithConfigure = (*yandexKmsAsymmetricSignatureKeyResource)(nil)
var _ resource.ResourceWithImportState = (*yandexKmsAsymmetricSignatureKeyResource)(nil)

type yandexKmsAsymmetricSignatureKeyResource struct {
	providerConfig *provider_config.Config
}

func NewResource() resource.Resource {
	return &yandexKmsAsymmetricSignatureKeyResource{}
}

func (r *yandexKmsAsymmetricSignatureKeyResource) Metadata(_ context.Context, req resource.MetadataRequest, resp *resource.MetadataResponse) {
	resp.TypeName = "yandex_kms_asymmetric_signature_key"
}

func (r *yandexKmsAsymmetricSignatureKeyResource) Configure(_ context.Context, req resource.ConfigureRequest, resp *resource.ConfigureResponse) {
	if req.ProviderData == nil {
		return
	}

	providerConfig, ok := req.ProviderData.(*provider_config.Config)
	if !ok {
		resp.Diagnostics.AddError(
			"Unexpected Resource Configure Type",
			fmt.Sprintf("Expected *provider_config.Config, got: %T. Please report this issue to the provider developers.", req.ProviderData),
		)
		return
	}

	r.providerConfig = providerConfig
}

func (r *yandexKmsAsymmetricSignatureKeyResource) Schema(ctx context.Context, _ resource.SchemaRequest, resp *resource.SchemaResponse) {
	resp.Schema = YandexKmsAsymmetricSignatureKeyResourceSchema(ctx)
}

func (r *yandexKmsAsymmetricSignatureKeyResource) ImportState(ctx context.Context, req resource.ImportStateRequest, resp *resource.ImportStateResponse) {
	resource.ImportStatePassthroughID(ctx, path.Root("asymmetric_signature_key_id"), req, resp)
}

func (r *yandexKmsAsymmetricSignatureKeyResource) Read(ctx context.Context, req resource.ReadRequest, resp *resource.ReadResponse) {
	var state yandexKmsAsymmetricSignatureKeyModel
	diags := req.State.Get(ctx, &state)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	readTimeout, timeoutInitError := state.Timeouts.Read(ctx, provider_config.DefaultTimeout)
	if timeoutInitError != nil {
		resp.Diagnostics.Append(timeoutInitError...)
		return
	}

	ctx, cancel := context.WithTimeout(ctx, readTimeout)
	defer cancel()

	reqApi := &asymmetricsignature.GetAsymmetricSignatureKeyRequest{}
	id := state.ID.ValueString()
	if !state.AsymmetricSignatureKeyId.IsUnknown() && !state.AsymmetricSignatureKeyId.IsNull() {
		id = state.AsymmetricSignatureKeyId.ValueString()
	}
	reqApi.SetKeyId(id)
	tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_signature_key request: %s", validate.ProtoDump(reqApi)))

	md := new(metadata.MD)
	res, err := asymmetricsignaturesdk.NewAsymmetricSignatureKeyClient(r.providerConfig.SDKv2).Get(ctx, reqApi, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_signature_key x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_signature_key x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		if validate.IsStatusWithCode(err, codes.NotFound) {
			resp.Diagnostics.AddWarning(
				"Failed to Read resource",
				"asymmetric_signature_key not found",
			)
			resp.State.RemoveResource(ctx)
			return
		} else {
			resp.Diagnostics.AddError(
				"Failed to Read resource",
				"Error while requesting API to get asymmetric_signature_key:"+err.Error(),
			)
		}
	}
	tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_signature_key response: %s", validate.ProtoDump(res)))

	if resp.Diagnostics.HasError() {
		return
	}

	// diagnostics don't have errors and resource is nil => resource not found
	if res == nil {
		resp.Diagnostics.AddError("Failed to read", "Resource not found")
		return
	}

	newState := flattenYandexKmsAsymmetricSignatureKey(ctx, res, state, state.Timeouts, &resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &newState)...)
	if resp.Diagnostics.HasError() {
		return
	}
}

func (r *yandexKmsAsymmetricSignatureKeyResource) Create(ctx context.Context, req resource.CreateRequest, resp *resource.CreateResponse) {
	var plan yandexKmsAsymmetricSignatureKeyModel
	diags := req.Plan.Get(ctx, &plan)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	createTimeout, timeoutInitError := plan.Timeouts.Create(ctx, provider_config.DefaultTimeout)
	if timeoutInitError != nil {
		resp.Diagnostics.Append(timeoutInitError...)
		return
	}

	ctx, cancel := context.WithTimeout(ctx, createTimeout)
	defer cancel()

	createReq := &asymmetricsignature.CreateAsymmetricSignatureKeyRequest{}
	createReq.SetFolderId(converter.GetFolderID(plan.FolderId.ValueString(), r.providerConfig, &diags))
	createReq.SetName(plan.Name.ValueString())
	createReq.SetDescription(plan.Description.ValueString())
	createReq.SetLabels(expandYandexKmsAsymmetricSignatureKeyLabels(ctx, plan.Labels, &diags))
	createReq.SetSignatureAlgorithm(asymmetricsignature.AsymmetricSignatureAlgorithm(asymmetricsignature.AsymmetricSignatureAlgorithm_value[plan.SignatureAlgorithm.ValueString()]))
	createReq.SetDeletionProtection(plan.DeletionProtection.ValueBool())
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}
	tflog.Debug(ctx, fmt.Sprintf("Create asymmetric_signature_key request: %s", validate.ProtoDump(createReq)))

	md := new(metadata.MD)
	op, err := asymmetricsignaturesdk.NewAsymmetricSignatureKeyClient(r.providerConfig.SDKv2).Create(ctx, createReq, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("[DEBUG] Create asymmetric_signature_key x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Create asymmetric_signature_key x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		resp.Diagnostics.AddError(
			"Failed to Create resource",
			"Error while requesting API to create asymmetric_signature_key:"+err.Error(),
		)
		return
	}
	createRes, err := op.Wait(ctx)
	if err != nil {
		resp.Diagnostics.AddError(
			"Unable to Create Resource",
			fmt.Sprintf("An unexpected error occurred while waiting longrunning response. "+
				"Please retry the operation or report this issue to the provider developers.\n\n"+
				"Error: %s", err),
		)
		return
	}
	tflog.Debug(ctx, fmt.Sprintf("Create asymmetric_signature_key response: %s", validate.ProtoDump(createRes)))

	plan.AsymmetricSignatureKeyId = types.StringValue(createRes.Id)

	reqApi := &asymmetricsignature.GetAsymmetricSignatureKeyRequest{}
	reqApi.SetKeyId(plan.AsymmetricSignatureKeyId.ValueString())
	tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_signature_key request: %s", validate.ProtoDump(reqApi)))

	md = new(metadata.MD)
	res, err := asymmetricsignaturesdk.NewAsymmetricSignatureKeyClient(r.providerConfig.SDKv2).Get(ctx, reqApi, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_signature_key x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_signature_key x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		if validate.IsStatusWithCode(err, codes.NotFound) {
			resp.Diagnostics.AddWarning(
				"Failed to Read resource",
				"asymmetric_signature_key not found",
			)
		} else {
			resp.Diagnostics.AddError(
				"Failed to Read resource",
				"Error while requesting API to get asymmetric_signature_key:"+err.Error(),
			)
		}
	}
	tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_signature_key response: %s", validate.ProtoDump(res)))

	if resp.Diagnostics.HasError() {
		return
	}

	// diagnostics don't have errors and resource is nil => resource not found
	if res == nil {
		resp.Diagnostics.AddError("Failed to read", "Resource not found")
		return
	}

	newState := flattenYandexKmsAsymmetricSignatureKey(ctx, res, plan, plan.Timeouts, &resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &newState)...)
	if resp.Diagnostics.HasError() {
		return
	}
}

func (r *yandexKmsAsymmetricSignatureKeyResource) Delete(ctx context.Context, req resource.DeleteRequest, resp *resource.DeleteResponse) {
	var state yandexKmsAsymmetricSignatureKeyModel
	diags := req.State.Get(ctx, &state)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	deleteTimeout, timeoutInitError := state.Timeouts.Delete(ctx, provider_config.DefaultTimeout)
	if timeoutInitError != nil {
		resp.Diagnostics.Append(timeoutInitError...)
		return
	}

	ctx, cancel := context.WithTimeout(ctx, deleteTimeout)
	defer cancel()

	reqApi := &asymmetricsignature.DeleteAsymmetricSignatureKeyRequest{}
	id := state.ID.ValueString()
	if !state.AsymmetricSignatureKeyId.IsUnknown() && !state.AsymmetricSignatureKeyId.IsNull() {
		id = state.AsymmetricSignatureKeyId.ValueString()
	}
	reqApi.SetKeyId(id)
	tflog.Debug(ctx, fmt.Sprintf("Delete asymmetric_signature_key request: %s", validate.ProtoDump(reqApi)))

	md := new(metadata.MD)

	op, err := asymmetricsignaturesdk.NewAsymmetricSignatureKeyClient(r.providerConfig.SDKv2).Delete(ctx, reqApi, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Delete asymmetric_signature_key x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Delete asymmetric_signature_key x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		resp.Diagnostics.AddError(
			"Failed to Delete resource",
			"Error while requesting API to delete asymmetric_signature_key:"+err.Error(),
		)
		return
	}
	deleteRes, err := op.Wait(ctx)
	if err != nil {
		resp.Diagnostics.AddError(
			"Unable to Delete Resource",
			fmt.Sprintf("An unexpected error occurred while waiting longrunning response. "+
				"Please retry the operation or report this issue to the provider developers.\n\n"+
				"Error: %s", err),
		)
		return
	}
	tflog.Debug(ctx, fmt.Sprintf("[DEBUG] Delete asymmetric_signature_key response: %s", validate.ProtoDump(deleteRes)))
}

func (r *yandexKmsAsymmetricSignatureKeyResource) Update(ctx context.Context, req resource.UpdateRequest, resp *resource.UpdateResponse) {
	var plan, state yandexKmsAsymmetricSignatureKeyModel
	diags := req.Plan.Get(ctx, &plan)
	resp.Diagnostics.Append(diags...)
	diags = req.State.Get(ctx, &state)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	updateTimeout, timeoutInitError := plan.Timeouts.Update(ctx, provider_config.DefaultTimeout)
	if timeoutInitError != nil {
		resp.Diagnostics.Append(timeoutInitError...)
		return
	}

	ctx, cancel := context.WithTimeout(ctx, updateTimeout)
	defer cancel()
	var updatePaths []string

	if !plan.AsymmetricSignatureKeyId.Equal(state.AsymmetricSignatureKeyId) {
		updatePaths = append(updatePaths, "key_id")
	}
	if !plan.DeletionProtection.Equal(state.DeletionProtection) {
		updatePaths = append(updatePaths, "deletion_protection")
	}
	if !plan.Description.Equal(state.Description) {
		updatePaths = append(updatePaths, "description")
	}
	if plan.Labels.IsNull() {
		plan.Labels = types.MapNull(types.StringType)
	}
	if state.Labels.IsNull() {
		state.Labels = types.MapNull(types.StringType)
	}
	if !plan.Labels.Equal(state.Labels) {
		updatePaths = append(updatePaths, "labels")
	}
	if !plan.Name.Equal(state.Name) {
		updatePaths = append(updatePaths, "name")
	}
	if !plan.Status.Equal(state.Status) {
		updatePaths = append(updatePaths, "status")
	}
	if len(updatePaths) != 0 {

		updateReq := &asymmetricsignature.UpdateAsymmetricSignatureKeyRequest{}
		id := plan.ID.ValueString()
		if !plan.AsymmetricSignatureKeyId.IsUnknown() && !plan.AsymmetricSignatureKeyId.IsNull() {
			id = plan.AsymmetricSignatureKeyId.ValueString()
		}
		updateReq.SetKeyId(id)
		updateReq.SetName(plan.Name.ValueString())
		updateReq.SetDescription(plan.Description.ValueString())
		updateReq.SetStatus(asymmetricsignature.AsymmetricSignatureKey_Status(asymmetricsignature.AsymmetricSignatureKey_Status_value[plan.Status.ValueString()]))
		updateReq.SetLabels(expandYandexKmsAsymmetricSignatureKeyLabels(ctx, plan.Labels, &diags))
		updateReq.SetDeletionProtection(plan.DeletionProtection.ValueBool())
		updateReq.SetUpdateMask(&field_mask.FieldMask{Paths: updatePaths})

		resp.Diagnostics.Append(diags...)
		if resp.Diagnostics.HasError() {
			return
		}
		tflog.Debug(ctx, fmt.Sprintf("Update asymmetric_signature_key request: %s", validate.ProtoDump(updateReq)))

		md := new(metadata.MD)
		op, err := asymmetricsignaturesdk.NewAsymmetricSignatureKeyClient(r.providerConfig.SDKv2).Update(ctx, updateReq, grpc.Header(md))
		if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
			tflog.Debug(ctx, fmt.Sprintf("Update asymmetric_signature_key x-server-trace-id: %s", traceHeader[0]))
		}
		if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
			tflog.Debug(ctx, fmt.Sprintf("Update asymmetric_signature_key x-server-request-id: %s", traceHeader[0]))
		}
		if err != nil {
			resp.Diagnostics.AddError(
				"Failed to Read resource",
				"Error while requesting API to update asymmetric_signature_key:"+err.Error(),
			)
			return
		}
		updateRes, err := op.Wait(ctx)
		if err != nil {
			resp.Diagnostics.AddError(
				"Unable to Update Resource",
				fmt.Sprintf("An unexpected error occurred while waiting longrunning response. "+
					"Please retry the operation or report this issue to the provider developers.\n\n"+
					"Error: %s", err),
			)
			return
		}
		tflog.Debug(ctx, fmt.Sprintf("Update asymmetric_signature_key response: %s", validate.ProtoDump(updateRes)))

		plan.AsymmetricSignatureKeyId = types.StringValue(updateRes.Id)
	}
	reqApi := &asymmetricsignature.GetAsymmetricSignatureKeyRequest{}
	reqApi.SetKeyId(plan.AsymmetricSignatureKeyId.ValueString())
	tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_signature_key request: %s", validate.ProtoDump(reqApi)))
	md := new(metadata.MD)
	res, err := asymmetricsignaturesdk.NewAsymmetricSignatureKeyClient(r.providerConfig.SDKv2).Get(ctx, reqApi, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_signature_key x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_signature_key x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		if validate.IsStatusWithCode(err, codes.NotFound) {
			resp.Diagnostics.AddWarning(
				"Failed to Read resource",
				"asymmetric_signature_key not found",
			)
		} else {
			resp.Diagnostics.AddError(
				"Failed to Read resource",
				"Error while requesting API to get asymmetric_signature_key:"+err.Error(),
			)
		}
	}
	tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_signature_key response: %s", validate.ProtoDump(res)))

	if resp.Diagnostics.HasError() {
		return
	}

	// diagnostics don't have errors and resource is nil => resource not found
	if res == nil {
		resp.Diagnostics.AddError("Failed to read", "Resource not found")
		return
	}

	to := state.Timeouts
	if !plan.Timeouts.Equal(state.Timeouts) {
		to = plan.Timeouts
	}

	newState := flattenYandexKmsAsymmetricSignatureKey(ctx, res, plan, to, &resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &newState)...)
	if resp.Diagnostics.HasError() {
		return
	}
}
