// Code generated by tfgen. DO NOT EDIT.

package yandex_kms_asymmetric_encryption_key

import (
	"context"
	"fmt"

	"github.com/hashicorp/terraform-plugin-framework/path"
	"github.com/hashicorp/terraform-plugin-framework/resource"
	"github.com/hashicorp/terraform-plugin-framework/types"
	"github.com/hashicorp/terraform-plugin-log/tflog"
	asymmetricencryption "github.com/yandex-cloud/go-genproto/yandex/cloud/kms/v1/asymmetricencryption"
	asymmetricencryptionsdk "github.com/yandex-cloud/go-sdk/services/kms/v1/asymmetricencryption"
	"github.com/yandex-cloud/terraform-provider-yandex/pkg/converter"
	"github.com/yandex-cloud/terraform-provider-yandex/pkg/validate"
	provider_config "github.com/yandex-cloud/terraform-provider-yandex/yandex-framework/provider/config"
	"google.golang.org/genproto/protobuf/field_mask"
	"google.golang.org/grpc"
	"google.golang.org/grpc/codes"
	"google.golang.org/grpc/metadata"
)

var _ resource.ResourceWithConfigure = (*yandexKmsAsymmetricEncryptionKeyResource)(nil)
var _ resource.ResourceWithImportState = (*yandexKmsAsymmetricEncryptionKeyResource)(nil)

type yandexKmsAsymmetricEncryptionKeyResource struct {
	providerConfig *provider_config.Config
}

func NewResource() resource.Resource {
	return &yandexKmsAsymmetricEncryptionKeyResource{}
}

func (r *yandexKmsAsymmetricEncryptionKeyResource) Metadata(_ context.Context, req resource.MetadataRequest, resp *resource.MetadataResponse) {
	resp.TypeName = "yandex_kms_asymmetric_encryption_key"
}

func (r *yandexKmsAsymmetricEncryptionKeyResource) Configure(_ context.Context, req resource.ConfigureRequest, resp *resource.ConfigureResponse) {
	if req.ProviderData == nil {
		return
	}

	providerConfig, ok := req.ProviderData.(*provider_config.Config)
	if !ok {
		resp.Diagnostics.AddError(
			"Unexpected Resource Configure Type",
			fmt.Sprintf("Expected *provider_config.Config, got: %T. Please report this issue to the provider developers.", req.ProviderData),
		)
		return
	}

	r.providerConfig = providerConfig
}

func (r *yandexKmsAsymmetricEncryptionKeyResource) Schema(ctx context.Context, _ resource.SchemaRequest, resp *resource.SchemaResponse) {
	resp.Schema = YandexKmsAsymmetricEncryptionKeyResourceSchema(ctx)
}

func (r *yandexKmsAsymmetricEncryptionKeyResource) ImportState(ctx context.Context, req resource.ImportStateRequest, resp *resource.ImportStateResponse) {
	resource.ImportStatePassthroughID(ctx, path.Root("asymmetric_encryption_key_id"), req, resp)
}

func (r *yandexKmsAsymmetricEncryptionKeyResource) Read(ctx context.Context, req resource.ReadRequest, resp *resource.ReadResponse) {
	var state yandexKmsAsymmetricEncryptionKeyModel
	diags := req.State.Get(ctx, &state)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	readTimeout, timeoutInitError := state.Timeouts.Read(ctx, provider_config.DefaultTimeout)
	if timeoutInitError != nil {
		resp.Diagnostics.Append(timeoutInitError...)
		return
	}

	ctx, cancel := context.WithTimeout(ctx, readTimeout)
	defer cancel()

	reqApi := &asymmetricencryption.GetAsymmetricEncryptionKeyRequest{}
	id := state.ID.ValueString()
	if !state.AsymmetricEncryptionKeyId.IsUnknown() && !state.AsymmetricEncryptionKeyId.IsNull() {
		id = state.AsymmetricEncryptionKeyId.ValueString()
	}
	reqApi.SetKeyId(id)

	tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_encryption_key request: %s", validate.ProtoDump(reqApi)))

	md := new(metadata.MD)
	res, err := asymmetricencryptionsdk.NewAsymmetricEncryptionKeyClient(r.providerConfig.SDKv2).Get(ctx, reqApi, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_encryption_key x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_encryption_key x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		if validate.IsStatusWithCode(err, codes.NotFound) {
			resp.Diagnostics.AddWarning(
				"Failed to Read resource",
				"asymmetric_encryption_key not found",
			)
			resp.State.RemoveResource(ctx)
			return
		} else {
			resp.Diagnostics.AddError(
				"Failed to Read resource",
				"Error while requesting API to get asymmetric_encryption_key:"+err.Error(),
			)
		}
	}

	tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_encryption_key response: %s", validate.ProtoDump(res)))

	if resp.Diagnostics.HasError() {
		return
	}

	// diagnostics don't have errors and resource is nil => resource not found
	if res == nil {
		resp.Diagnostics.AddError("Failed to read", "Resource not found")
		return
	}

	newState := flattenYandexKmsAsymmetricEncryptionKey(ctx, res, state, state.Timeouts, &resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &newState)...)
	if resp.Diagnostics.HasError() {
		return
	}
}

func (r *yandexKmsAsymmetricEncryptionKeyResource) Create(ctx context.Context, req resource.CreateRequest, resp *resource.CreateResponse) {
	var plan yandexKmsAsymmetricEncryptionKeyModel
	diags := req.Plan.Get(ctx, &plan)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	createTimeout, timeoutInitError := plan.Timeouts.Create(ctx, provider_config.DefaultTimeout)
	if timeoutInitError != nil {
		resp.Diagnostics.Append(timeoutInitError...)
		return
	}

	ctx, cancel := context.WithTimeout(ctx, createTimeout)
	defer cancel()

	createReq := &asymmetricencryption.CreateAsymmetricEncryptionKeyRequest{}
	createReq.SetFolderId(converter.GetFolderID(plan.FolderId.ValueString(), r.providerConfig, &diags))
	createReq.SetName(plan.Name.ValueString())
	createReq.SetDescription(plan.Description.ValueString())
	createReq.SetLabels(expandYandexKmsAsymmetricEncryptionKeyLabels(ctx, plan.Labels, &diags))
	createReq.SetEncryptionAlgorithm(asymmetricencryption.AsymmetricEncryptionAlgorithm(asymmetricencryption.AsymmetricEncryptionAlgorithm_value[plan.EncryptionAlgorithm.ValueString()]))
	createReq.SetDeletionProtection(plan.DeletionProtection.ValueBool())
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	tflog.Debug(ctx, fmt.Sprintf("Create asymmetric_encryption_key request: %s", validate.ProtoDump(createReq)))

	md := new(metadata.MD)
	op, err := asymmetricencryptionsdk.NewAsymmetricEncryptionKeyClient(r.providerConfig.SDKv2).Create(ctx, createReq, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("[DEBUG] Create asymmetric_encryption_key x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Create asymmetric_encryption_key x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		resp.Diagnostics.AddError(
			"Failed to Create resource",
			"Error while requesting API to create asymmetric_encryption_key:"+err.Error(),
		)
		return
	}
	createRes, err := op.Wait(ctx)
	if err != nil {
		resp.Diagnostics.AddError(
			"Unable to Create Resource",
			fmt.Sprintf("An unexpected error occurred while waiting longrunning response. "+
				"Please retry the operation or report this issue to the provider developers.\n\n"+
				"Error: %s", err),
		)
		return
	}

	tflog.Debug(ctx, fmt.Sprintf("Create asymmetric_encryption_key response: %s", validate.ProtoDump(createRes)))

	plan.AsymmetricEncryptionKeyId = types.StringValue(createRes.Id)

	reqApi := &asymmetricencryption.GetAsymmetricEncryptionKeyRequest{}
	reqApi.SetKeyId(plan.AsymmetricEncryptionKeyId.ValueString())

	tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_encryption_key request: %s", validate.ProtoDump(reqApi)))

	md = new(metadata.MD)
	res, err := asymmetricencryptionsdk.NewAsymmetricEncryptionKeyClient(r.providerConfig.SDKv2).Get(ctx, reqApi, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_encryption_key x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_encryption_key x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		if validate.IsStatusWithCode(err, codes.NotFound) {
			resp.Diagnostics.AddWarning(
				"Failed to Read resource",
				"asymmetric_encryption_key not found",
			)
		} else {
			resp.Diagnostics.AddError(
				"Failed to Read resource",
				"Error while requesting API to get asymmetric_encryption_key:"+err.Error(),
			)
		}
	}

	tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_encryption_key response: %s", validate.ProtoDump(res)))

	if resp.Diagnostics.HasError() {
		return
	}

	// diagnostics don't have errors and resource is nil => resource not found
	if res == nil {
		resp.Diagnostics.AddError("Failed to read", "Resource not found")
		return
	}

	newState := flattenYandexKmsAsymmetricEncryptionKey(ctx, res, plan, plan.Timeouts, &resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &newState)...)
	if resp.Diagnostics.HasError() {
		return
	}
}

func (r *yandexKmsAsymmetricEncryptionKeyResource) Delete(ctx context.Context, req resource.DeleteRequest, resp *resource.DeleteResponse) {
	var state yandexKmsAsymmetricEncryptionKeyModel
	diags := req.State.Get(ctx, &state)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	deleteTimeout, timeoutInitError := state.Timeouts.Delete(ctx, provider_config.DefaultTimeout)
	if timeoutInitError != nil {
		resp.Diagnostics.Append(timeoutInitError...)
		return
	}

	ctx, cancel := context.WithTimeout(ctx, deleteTimeout)
	defer cancel()

	reqApi := &asymmetricencryption.DeleteAsymmetricEncryptionKeyRequest{}
	id := state.ID.ValueString()
	if !state.AsymmetricEncryptionKeyId.IsUnknown() && !state.AsymmetricEncryptionKeyId.IsNull() {
		id = state.AsymmetricEncryptionKeyId.ValueString()
	}
	reqApi.SetKeyId(id)

	tflog.Debug(ctx, fmt.Sprintf("Delete asymmetric_encryption_key request: %s", validate.ProtoDump(reqApi)))

	md := new(metadata.MD)

	op, err := asymmetricencryptionsdk.NewAsymmetricEncryptionKeyClient(r.providerConfig.SDKv2).Delete(ctx, reqApi, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Delete asymmetric_encryption_key x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Delete asymmetric_encryption_key x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		resp.Diagnostics.AddError(
			"Failed to Delete resource",
			"Error while requesting API to delete asymmetric_encryption_key:"+err.Error(),
		)
		return
	}
	deleteRes, err := op.Wait(ctx)
	if err != nil {
		resp.Diagnostics.AddError(
			"Unable to Delete Resource",
			fmt.Sprintf("An unexpected error occurred while waiting longrunning response. "+
				"Please retry the operation or report this issue to the provider developers.\n\n"+
				"Error: %s", err),
		)
		return
	}

	tflog.Debug(ctx, fmt.Sprintf("[DEBUG] Delete asymmetric_encryption_key response: %s", validate.ProtoDump(deleteRes)))
}

func (r *yandexKmsAsymmetricEncryptionKeyResource) Update(ctx context.Context, req resource.UpdateRequest, resp *resource.UpdateResponse) {
	var plan, state yandexKmsAsymmetricEncryptionKeyModel
	diags := req.Plan.Get(ctx, &plan)
	resp.Diagnostics.Append(diags...)
	diags = req.State.Get(ctx, &state)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	updateTimeout, timeoutInitError := plan.Timeouts.Update(ctx, provider_config.DefaultTimeout)
	if timeoutInitError != nil {
		resp.Diagnostics.Append(timeoutInitError...)
		return
	}

	ctx, cancel := context.WithTimeout(ctx, updateTimeout)
	defer cancel()
	var updatePaths []string

	if !plan.AsymmetricEncryptionKeyId.Equal(state.AsymmetricEncryptionKeyId) {
		updatePaths = append(updatePaths, "key_id")
	}
	if !plan.DeletionProtection.Equal(state.DeletionProtection) {
		updatePaths = append(updatePaths, "deletion_protection")
	}
	if !plan.Description.Equal(state.Description) {
		updatePaths = append(updatePaths, "description")
	}
	if plan.Labels.IsNull() {
		plan.Labels = types.MapNull(types.StringType)
	}
	if state.Labels.IsNull() {
		state.Labels = types.MapNull(types.StringType)
	}
	if !plan.Labels.Equal(state.Labels) {
		updatePaths = append(updatePaths, "labels")
	}
	if !plan.Name.Equal(state.Name) {
		updatePaths = append(updatePaths, "name")
	}
	if !plan.Status.Equal(state.Status) {
		updatePaths = append(updatePaths, "status")
	}
	if len(updatePaths) != 0 {

		updateReq := &asymmetricencryption.UpdateAsymmetricEncryptionKeyRequest{}
		id := plan.ID.ValueString()
		if !plan.AsymmetricEncryptionKeyId.IsUnknown() && !plan.AsymmetricEncryptionKeyId.IsNull() {
			id = plan.AsymmetricEncryptionKeyId.ValueString()
		}
		updateReq.SetKeyId(id)
		updateReq.SetName(plan.Name.ValueString())
		updateReq.SetDescription(plan.Description.ValueString())
		updateReq.SetStatus(asymmetricencryption.AsymmetricEncryptionKey_Status(asymmetricencryption.AsymmetricEncryptionKey_Status_value[plan.Status.ValueString()]))
		updateReq.SetLabels(expandYandexKmsAsymmetricEncryptionKeyLabels(ctx, plan.Labels, &diags))
		updateReq.SetDeletionProtection(plan.DeletionProtection.ValueBool())
		updateReq.SetUpdateMask(&field_mask.FieldMask{Paths: updatePaths})

		resp.Diagnostics.Append(diags...)
		if resp.Diagnostics.HasError() {
			return
		}

		tflog.Debug(ctx, fmt.Sprintf("Update asymmetric_encryption_key request: %s", validate.ProtoDump(updateReq)))

		md := new(metadata.MD)
		op, err := asymmetricencryptionsdk.NewAsymmetricEncryptionKeyClient(r.providerConfig.SDKv2).Update(ctx, updateReq, grpc.Header(md))
		if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
			tflog.Debug(ctx, fmt.Sprintf("Update asymmetric_encryption_key x-server-trace-id: %s", traceHeader[0]))
		}
		if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
			tflog.Debug(ctx, fmt.Sprintf("Update asymmetric_encryption_key x-server-request-id: %s", traceHeader[0]))
		}
		if err != nil {
			resp.Diagnostics.AddError(
				"Failed to Read resource",
				"Error while requesting API to update asymmetric_encryption_key:"+err.Error(),
			)
			return
		}
		updateRes, err := op.Wait(ctx)
		if err != nil {
			resp.Diagnostics.AddError(
				"Unable to Update Resource",
				fmt.Sprintf("An unexpected error occurred while waiting longrunning response. "+
					"Please retry the operation or report this issue to the provider developers.\n\n"+
					"Error: %s", err),
			)
			return
		}

		tflog.Debug(ctx, fmt.Sprintf("Update asymmetric_encryption_key response: %s", validate.ProtoDump(updateRes)))

		plan.AsymmetricEncryptionKeyId = types.StringValue(updateRes.Id)
	}
	reqApi := &asymmetricencryption.GetAsymmetricEncryptionKeyRequest{}
	reqApi.SetKeyId(plan.AsymmetricEncryptionKeyId.ValueString())

	tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_encryption_key request: %s", validate.ProtoDump(reqApi)))
	md := new(metadata.MD)
	res, err := asymmetricencryptionsdk.NewAsymmetricEncryptionKeyClient(r.providerConfig.SDKv2).Get(ctx, reqApi, grpc.Header(md))
	if traceHeader := md.Get("x-server-trace-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_encryption_key x-server-trace-id: %s", traceHeader[0]))
	}
	if traceHeader := md.Get("x-server-request-id"); len(traceHeader) > 0 {
		tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_encryption_key x-server-request-id: %s", traceHeader[0]))
	}
	if err != nil {
		if validate.IsStatusWithCode(err, codes.NotFound) {
			resp.Diagnostics.AddWarning(
				"Failed to Read resource",
				"asymmetric_encryption_key not found",
			)
		} else {
			resp.Diagnostics.AddError(
				"Failed to Read resource",
				"Error while requesting API to get asymmetric_encryption_key:"+err.Error(),
			)
		}
	}

	tflog.Debug(ctx, fmt.Sprintf("Read asymmetric_encryption_key response: %s", validate.ProtoDump(res)))

	if resp.Diagnostics.HasError() {
		return
	}

	// diagnostics don't have errors and resource is nil => resource not found
	if res == nil {
		resp.Diagnostics.AddError("Failed to read", "Resource not found")
		return
	}

	to := state.Timeouts
	if !plan.Timeouts.Equal(state.Timeouts) {
		to = plan.Timeouts
	}

	newState := flattenYandexKmsAsymmetricEncryptionKey(ctx, res, plan, to, &resp.Diagnostics)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &newState)...)
	if resp.Diagnostics.HasError() {
		return
	}
}
